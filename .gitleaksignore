# Superseded by .gitleaks.toml's regex allowlist (Phase 18 CI fix): a
# fingerprint here is pinned to one exact commit+line and breaks the
# moment the same known-safe string appears anywhere else — exactly
# what happened when docs/demo-script.md reused the same documented
# dev-only placeholder README.md already used. The regex allowlist in
# .gitleaks.toml covers the string itself, wherever it legitimately
# appears, without this fragility. Kept as an available mechanism for
# a future one-off false positive that doesn't fit a general pattern.
